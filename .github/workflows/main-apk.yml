name: Main APK (internal)

on:
  push:
    branches: [ main ]

jobs:
  build-apk:
    runs-on: ubuntu-latest
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      EXPO_PUBLIC_API_BASE_URL: ${{ secrets.EXPO_PUBLIC_API_BASE_URL }}
      EXPO_PUBLIC_VERSION: ${{ secrets.EXPO_PUBLIC_VERSION }}

    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: 📦 Install deps
        run: npm ci

      - name: 🔑 Verify EAS auth
        run: npx --yes eas-cli@latest whoami || true

      - name: 🧩 Write .env
        run: |
          echo "EXPO_PUBLIC_API_BASE_URL=${EXPO_PUBLIC_API_BASE_URL}" >> .env
          echo "EXPO_PUBLIC_VERSION=${EXPO_PUBLIC_VERSION}" >> .env

      - name: 📲 EAS build (APK, preview)
        run: npx --yes eas-cli@latest build --platform android --profile preview --non-interactive --wait

      - name: ⏳ Wait for build
        run: npx --yes eas-cli@latest build:wait

      - name: ⬇️ Download latest APK
        run: npx --yes eas-cli@latest build:download --latest --platform android --profile preview -o app-main.apk

      - name: 📤 Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-main
          path: app-main.apk
          retention-days: 30
